[pytest]
addopts = -ra --strict-markers
markers =
    contract: requires a running merchant API
    sandbox: requires the deterministic sandbox simulation API
    security: authentication, tamper, replay, and consumer-boundary checks
