This directory is intentionally empty of credentials. For the opt-in legacy
profile, mount root-owned mode-0400 files whose basenames exactly match the
approved `legacy_secret_ref` and `core_secret_ref` database values.
